<%= render 'shared/navbar'%>

<p id="notice"><%= notice %></p>
<div class = "container pb-3 pt-4">

  <h3>
  <%= link_to 'Todos', users_path, class: 'badge badge-pill badge-success' if @core.nil? %>
  <%= link_to 'Todos', users_path, class: 'badge badge-pill badge-secondary' if !@core.nil? %>

  <%= link_to 'NDP', users_path(core: "NDP"), class: 'badge badge-pill badge-success' if @core == "NDP" %>
  <%= link_to 'NDP', users_path(core: "NDP"), class: 'badge badge-pill badge-secondary' if @core != "NDP" %>

  <%= link_to 'NUT', users_path(core: "NUT"), class: 'badge badge-pill badge-success' if @core == "NUT" %>
  <%= link_to 'NUT', users_path(core: "NUT"), class: 'badge badge-pill badge-secondary' if @core != "NUT" %>

  <%= link_to 'NOE', users_path(core: "NOE"), class: 'badge badge-pill badge-success' if @core == "NOE" %>
  <%= link_to 'NOE', users_path(core: "NOE"), class: 'badge badge-pill badge-secondary' if @core != "NOE" %>

  <%= link_to 'NAT', users_path(core: "NAT"), class: 'badge badge-pill badge-success' if @core == "NAT" %>
  <%= link_to 'NAT', users_path(core: "NAT"), class: 'badge badge-pill badge-secondary' if @core != "NAT" %>
  
  <%= link_to 'NUMAR', users_path(core: "NUMAR"), class: 'badge badge-pill badge-success' if @core == "NUMAR" %>
  <%= link_to 'NUMAR', users_path(core: "NUMAR"), class: 'badge badge-pill badge-secondary' if @core != "NUMAR" %>

  <%= link_to 'Trainees', users_path(core: "nenhum"), class: 'badge badge-pill badge-success' if @core == "nenhum" %>
  <%= link_to 'Trainees', users_path(core: "nenhum"), class: 'badge badge-pill badge-secondary' if @core != "nenhum" %>

  <%= link_to 'PRES', users_path(core: "PRES"), class: 'badge badge-pill badge-success' if @core == "PRES" %>
  <%= link_to 'PRES', users_path(core: "PRES"), class: 'badge badge-pill badge-secondary' if @core != "PRES" %>
  </h3>

  <br>
  <table class ="table table-responsive-sm table-responsive-md table-striped" >
    <thead>
      <tr>
        <th>Nome</th>
        <th>Email</th>
        <th>Membro</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @users.order(:nome).each do |user| %>
        <tr>
          <td><%= user.nome %></td>
          <td><%= user.email %></td>
          <td>
            <% if user.membro == true %>
              <span class="badge badge-success mr-1 d-block">Membro</span>
            <% end %>
            <% if user.membro == false %>
              <span class="badge badge-danger mr-1 d-block">Trainee</span>
            <% end %>
        </td>
          <td><%= link_to 'Histórico', user_path(user), class: 'text-primary btn-sm' %></td>
          <td><%= link_to 'Editar', edit_user_path(user), class: 'text-primary btn-sm' %></td>
          <td><%= link_to 'Excluir', user, method: :delete, data: { confirm: 'Are you sure?' }, class:"text-danger btn-sm" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <div class="d-flex w-100 justify-content-center my-4">
    <%= link_to 'Novo', new_user_path, class: 'btn btn-primary mr-3 mb-4 px-5' %>
  </div>
</div>