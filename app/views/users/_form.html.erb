  <%= form_with(model: user, local: true) do |form| %>
    <% if user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

        <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  <div class = "container pb-3 pt-4">
    <div class= "row">
      <div class="field col">
        <%= form.label :nome %>
        <%= form.text_field :nome, class: "form-control" %>
      </div>

      <div class="field col">
        <%= form.label :email %>
        <%= form.text_field :email, class: "form-control" %>
      </div>
  </div>
  <br>
  <div class = "row">
      <div class="field col">
      <%= form.check_box :membro, class: "form-check-label" %>
        <%= form.label :membro %>
      </div>
  </div>
  <br>
      <div class="actions">
        <%= form.submit 'Criar', class: "btn btn-success buttons-new" if current_page?('/users/new')%>
        <%= form.submit 'Atualizar', class: "btn btn-success buttons-new" if !current_page?('/users/new')%>
      </div>
    <% end %>
  </div>
</div>