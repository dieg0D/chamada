<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class = "container">
        <%= link_to image_tag("Logo_CJR.png", border: 0, width: "75px", height: "100%", alt: "Chamada", padding: 0, margin: 0 ), root_path , class: 'navbar-brand' , id: "something" %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto mx-2">
            <li class="nav-item">
                <%= link_to "Inicio", attendance_lists_path, class: "nav-link" if !current_page?('/attendance_lists') && !current_page?('/') %>
                <%= link_to "Inicio", attendance_lists_path, class: "text-primary nav-link active" if current_page?('/attendance_lists') || current_page?('/') %>
            </li>
            <li class="nav-item">
                <%= link_to "Lista de Membros", reger_path, class: "nav-link" if !current_page?('/reger')%>
                <%= link_to "Lista de Membros", reger_path, class: "text-primary nav-link active" if current_page?('/reger')%>
            </li>
            <li class="nav-item">
                <%= link_to "Lista de Trainees", pt_path, class: "nav-link" if !current_page?('/pt')%>
                <%= link_to "Lista de Trainees", pt_path, class: "text-primary nav-link active" if current_page?('/pt')%>
            </li>
            <li class="nav-item">
                <%= link_to "Todos / Núcleos", users_path, class: "nav-link" if !current_page?('/users')%>
                <%= link_to "Todos / Núcleos", users_path, class: "text-primary nav-link active" if current_page?('/users')%>
            </li>
            <li class="nav-item">
                <%= link_to "Administradores", admins_path, class: "nav-link" if !request.env['PATH_INFO'].include?('/admins')%>
                <%= link_to "Administradores", admins_path, class: "text-warning nav-link active" if request.env['PATH_INFO'].include?('/admins')%>
            </li>
            </ul>
            <!--
            <form class="form-inline my-2 my-lg-0">
                <input class="form-control mr-sm-2" type="search" placeholder="Pesquisar por Data (em Breve)" aria-label="Pesquisar Membro">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Pesquisar</button>
            </form>
            -->
             <div class="nav-item">
                <%= link_to 'Sair', destroy_admin_session_path, method: :delete, class: "text-danger" %>
            </div>
        </div>
    </div>
</nav>